import tts_netflow_a
from setuptools import setup, find_packages

setup(
   name = 'tts_netflow_a',
   packages = find_packages(),
   version = tts_netflow_a.__version__,
   author = 'Pete Cacioppi',
   url = 'https://github.com/ticdat/tts_netflow_a',
   platforms = 'any',
)